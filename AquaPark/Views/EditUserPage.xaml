<Page x:Class="AquaPark.Views.EditUserPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:services="clr-namespace:AquaPark.Services"
      Title="EditUserPage">

    <ScrollViewer Background="#EAF7FB">
        <Border Width="520" Background="White" CornerRadius="18" Padding="35"
                HorizontalAlignment="Center" Margin="0 30">
            <StackPanel>
                <TextBlock Text="Изменение пользователя"
                           FontSize="26"
                           FontWeight="Bold"
                           Foreground="#16697A"
                           HorizontalAlignment="Center"
                           Margin="0 0 0 25"/>

                <TextBlock Text="Логин" Margin="0 0 0 6"/>
                <TextBox Text="{Binding Login, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}" Margin="0 0 0 12"/>

                <TextBlock Text="Пароль" Margin="0 0 0 6"/>
                <TextBox Text="{Binding Password, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}" Margin="0 0 0 12"/>

                <TextBlock Text="ФИО" Margin="0 0 0 6"/>
                <TextBox Text="{Binding FullName, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}" Margin="0 0 0 12"/>

                <TextBlock Text="Роль" Margin="0 0 0 6"/>
                <ComboBox ItemsSource="{Binding Roles}"
                          SelectedItem="{Binding SelectedRole, ValidatesOnNotifyDataErrors=True}"
                          DisplayMemberPath="RoleName"
                          Margin="0 0 0 12"/>

                <TextBlock Text="Телефон" Margin="0 0 0 6"/>
                <TextBox Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}"
                         services:InputMaskBehavior.PhoneOnly="True"
                         MaxLength="20"
                         Margin="0 0 0 12"/>

                <TextBlock Text="Email" Margin="0 0 0 6"/>
                <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Margin="0 0 0 12"/>

                <CheckBox Content="Активен" IsChecked="{Binding IsActive}" Margin="0 0 0 12"/>
                <CheckBox Content="Карточка сотрудника" IsChecked="{Binding IsEmployee}" Margin="0 0 0 12"/>

                <TextBlock Text="Должность" Margin="0 0 0 6"/>
                <TextBox Text="{Binding Position, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}" Margin="0 0 0 12"/>

                <TextBlock Text="Дата приема" Margin="0 0 0 6"/>
                <DatePicker SelectedDate="{Binding HireDate, ValidatesOnNotifyDataErrors=True}" Margin="0 0 0 12"/>

                <TextBlock Text="Зарплата" Margin="0 0 0 6"/>
                <TextBox Text="{Binding Salary, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}"
                         services:InputMaskBehavior.DecimalOnly="True"
                         Margin="0 0 0 18"/>

                <TextBlock Text="{Binding ErrorMessage}"
                           Foreground="Red"
                           FontSize="13"
                           TextAlignment="Center"
                           TextWrapping="Wrap"
                           Margin="0 0 0 12"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button Content="Сохранить" Width="130" Height="38" Margin="0 0 10 0"
                            Background="#16697A" Foreground="White" BorderThickness="0"
                            Cursor="Hand" Command="{Binding SaveCommand}"/>
                    <Button Content="Отмена" Width="130" Height="38"
                            Background="#D62828" Foreground="White" BorderThickness="0"
                            Cursor="Hand" Command="{Binding BackCommand}"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </ScrollViewer>
</Page>
